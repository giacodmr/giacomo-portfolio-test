<div class="retail-report" role="img" aria-label="Synthetic retail performance report card combining a store distribution histogram, horizontal average-ticket bars, and a purchase-frequency dot plot">
  <section class="viz-panel distribution-panel">
    <header><span>01</span><strong>Store performance distribution</strong></header>
    <div class="histogram" aria-hidden="true">
      <i style="--h: 27%"></i><i style="--h: 46%"></i><i style="--h: 69%"></i><i style="--h: 88%"></i><i style="--h: 100%"></i><i style="--h: 82%"></i><i style="--h: 61%"></i><i class="focus" style="--h: 39%"></i><i class="focus-strong" style="--h: 22%"></i>
    </div>
    <div class="distribution-axis" aria-hidden="true"><span>Lower</span><span>Core</span><span>Top</span></div>
    <p><b>Top 20%</b> of stores separated from the network baseline.</p>
  </section>

  <section class="viz-panel ticket-panel">
    <header><span>02</span><strong>Average ticket index</strong></header>
    <div class="horizontal-bars" aria-hidden="true">
      <div class="hbar-row"><span>Low</span><i><b style="--w: 68%"></b></i><strong>82</strong></div>
      <div class="hbar-row"><span>Core</span><i><b style="--w: 78%"></b></i><strong>94</strong></div>
      <div class="hbar-row"><span>High</span><i><b class="accent" style="--w: 89%"></b></i><strong>108</strong></div>
      <div class="hbar-row"><span>Top</span><i><b class="focus" style="--w: 100%"></b></i><strong>121</strong></div>
    </div>
    <p>Index increases consistently across performance clusters.</p>
  </section>

  <section class="viz-panel frequency-panel">
    <header><span>03</span><strong>Purchase frequency index</strong></header>
    <div class="dotplot-wrap" aria-hidden="true">
      <svg viewBox="0 0 260 132" preserveAspectRatio="none">
        <g class="grid-lines"><line x1="16" x2="244" y1="104" y2="104" /><line x1="16" x2="244" y1="67" y2="67" /><line x1="16" x2="244" y1="30" y2="30" /></g>
        <polyline class="trend-line" points="28,96 92,72 156,52 224,26" />
        <circle class="dot neutral" cx="28" cy="96" r="5.5" /><circle class="dot neutral" cx="92" cy="72" r="5.5" /><circle class="dot accent" cx="156" cy="52" r="6" /><circle class="dot focus" cx="224" cy="26" r="6.5" />
        <text x="28" y="82">75</text><text x="92" y="58">91</text><text x="156" y="38">104</text><text x="224" y="13">118</text>
      </svg>
      <div class="dotplot-axis"><span>Low</span><span>Core</span><span>High</span><span>Top</span></div>
    </div>
    <p>Higher-performing clusters show stronger repeat-purchase behaviour.</p>
  </section>

  <p class="report-note">Illustrative reconstruction with synthetic values.</p>
</div>

<style>
  .retail-report {
    width: 100%;
    display: grid;
    grid-template-columns: 1.05fr 1fr 1.05fr;
    gap: 0;
    margin-top: 0;
    border-top: 1px solid var(--ds-border-light, #eaeef2);
    border-bottom: 1px solid var(--ds-border-light, #eaeef2);
  }
  .viz-panel { min-width: 0; padding: 26px 28px 24px; border-right: 1px solid var(--ds-border-light, #eaeef2); }
  .viz-panel:first-child { padding-left: 0; }
  .viz-panel:nth-child(3) { padding-right: 0; border-right: 0; }
  .viz-panel header { display: flex; align-items: baseline; gap: 10px; min-height: 42px; }
  .viz-panel header span { color: var(--ds-subtle, #6e7781); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .64rem; font-weight: 650; letter-spacing: .12em; }
  .viz-panel header strong { color: var(--ds-muted, #57606a); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .8rem; font-weight: 650; line-height: 1.3; }
  .viz-panel > p { min-height: 40px; margin: 16px 0 0; color: var(--ds-subtle, #6e7781); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .67rem; line-height: 1.45; }
  .viz-panel > p b { color: var(--ds-ink, #1f2328); font-weight: 650; }
  .histogram { height: 158px; display: flex; align-items: end; gap: 5px; padding-top: 16px; }
  .histogram i { flex: 1; height: var(--h); min-height: 12px; background: var(--ds-data-context, #c1c7cd); border-radius: 3px 3px 0 0; }
  .histogram i:nth-child(4), .histogram i:nth-child(5), .histogram i:nth-child(6) { background: color-mix(in srgb, var(--ds-data-secondary, #78a9ff) 55%, var(--ds-data-context, #c1c7cd)); }
  .histogram i.focus { background: var(--ds-data-secondary, #78a9ff); }
  .histogram i.focus-strong { background: var(--ds-data-focus, #0f62fe); }
  .distribution-axis, .dotplot-axis { display: flex; justify-content: space-between; margin-top: 9px; color: var(--ds-subtle, #6e7781); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .59rem; }
  .horizontal-bars { display: grid; gap: 13px; padding-top: 19px; min-height: 167px; align-content: center; }
  .hbar-row { display: grid; grid-template-columns: 34px 1fr 30px; align-items: center; gap: 9px; min-width: 0; }
  .hbar-row > span, .hbar-row > strong { color: var(--ds-subtle, #6e7781); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .63rem; font-weight: 550; }
  .hbar-row > strong { color: var(--ds-ink, #1f2328); text-align: right; font-weight: 650; }
  .hbar-row > i { position: relative; height: 10px; overflow: hidden; border-radius: 3px; background: var(--ds-surface-soft, #f4f4f4); }
  .hbar-row > i > b { display: block; width: var(--w); height: 100%; border-radius: inherit; background: var(--ds-data-context, #c1c7cd); }
  .hbar-row > i > b.accent { background: var(--ds-data-secondary, #78a9ff); }
  .hbar-row > i > b.focus { background: var(--ds-data-focus, #0f62fe); }
  .dotplot-wrap { padding-top: 10px; }
  .dotplot-wrap svg { display: block; width: 100%; height: 158px; overflow: visible; }
  .grid-lines line { stroke: var(--ds-border-light, #eaeef2); stroke-width: 1; vector-effect: non-scaling-stroke; }
  .trend-line { fill: none; stroke: var(--ds-data-secondary, #78a9ff); stroke-width: 2; vector-effect: non-scaling-stroke; }
  .dot { stroke: var(--ds-surface, #ffffff); stroke-width: 2.2; vector-effect: non-scaling-stroke; }
  .dot.neutral { fill: var(--ds-data-context, #c1c7cd); } .dot.accent { fill: var(--ds-data-secondary, #78a9ff); } .dot.focus { fill: var(--ds-data-focus, #0f62fe); }
  .dotplot-wrap text { fill: var(--ds-ink, #1f2328); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: 10px; font-weight: 650; text-anchor: middle; }
  .dotplot-axis { display: grid; grid-template-columns: repeat(4, 1fr); text-align: center; margin-top: 2px; }
  .report-note { grid-column: 1 / -1; margin: 0; padding: 12px 0; border-top: 1px solid var(--ds-border-light, #eaeef2); color: var(--ds-subtle, #6e7781); font-family: var(--ds-font-sans, 'Mona Sans', sans-serif); font-size: .62rem; }
  @media (max-width: 760px) {
    .retail-report { grid-template-columns: 1fr; border-bottom: 0; }
    .viz-panel, .viz-panel:first-child, .viz-panel:nth-child(3) { padding: 22px 0; border-right: 0; border-bottom: 1px solid var(--ds-border-light, #eaeef2); }
    .report-note { padding-block: 12px; }
  }
</style>
