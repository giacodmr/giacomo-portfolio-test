<header class="site-header">
  <div class="container nav-inner">
    <a class="wordmark" href="/" aria-label="Giacomo Di Muro home">
      <span class="wordmark-mark" aria-hidden="true">G</span>
      <span>Giacomo Di Muro</span>
    </a>

    <nav aria-label="Primary navigation">
      <a href="/#work">Work</a>
      <a href="/about">About</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    z-index: 50;
    top: 0;
    min-height: 66px;
    border-bottom: 1px solid var(--border);
    background: color-mix(in srgb, var(--surface) 94%, transparent);
    backdrop-filter: blur(12px);
  }
  .nav-inner {
    min-height: 66px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
  }
  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: .98rem;
    font-weight: 650;
    letter-spacing: -.015em;
  }
  .wordmark-mark {
    display: grid;
    place-items: center;
    width: 29px;
    height: 29px;
    border: 1px solid var(--border);
    border-radius: 50%;
    background: var(--surface-soft);
    font-size: .78rem;
  }
  nav { display: flex; align-items: center; gap: clamp(20px, 2.6vw, 36px); }
  nav a {
    color: var(--text-secondary);
    font-size: .9rem;
    font-weight: 560;
    transition: color var(--ds-motion-fast) ease;
  }
  nav a:hover,
  nav a:focus-visible { color: var(--text); }

  @media (max-width: 620px) {
    .site-header { position: relative; }
    .nav-inner { min-height: auto; padding-top: 15px; padding-bottom: 15px; }
    .wordmark > span:last-child { display: none; }
  }
</style>
