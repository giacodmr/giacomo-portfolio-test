<div class="workflow-preview" role="img" aria-label="AI-enabled consulting workflow preview">
  <div class="workflow-step">
    <span>01</span>
    <strong>Business question</strong>
    <small>Objectives · Context · Constraints</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>02</span>
    <strong>Data & documents</strong>
    <small>Internal data · Reports · Materials</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step workflow-step-accent">
    <span>03</span>
    <strong>AI-supported analysis</strong>
    <small>Structure · Analyse · Synthesise</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>04</span>
    <strong>Consultant review</strong>
    <small>Validate · Refine · Apply context</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>05</span>
    <strong>Output / decision</strong>
    <small>Insights · Recommendations · Action</small>
  </div>
</div>

<style>
  .workflow-preview {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr auto 1fr;
    gap: 8px;
    align-items: stretch;
    margin-top: 22px;
  }

  .workflow-step {
    min-width: 0;
    min-height: 148px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 16px;
    padding: 14px;
    border: 1px solid rgba(241,246,242,.15);
    border-radius: 10px;
    background: rgba(255,255,255,.025);
  }

  .workflow-step span {
    color: rgba(240,245,241,.42);
    font-size: .57rem;
    letter-spacing: .12em;
  }

  .workflow-step strong {
    color: #f5f7f3;
    font-size: .78rem;
    font-weight: 500;
    line-height: 1.2;
  }

  .workflow-step small {
    color: rgba(240,245,241,.52);
    font-size: .58rem;
    line-height: 1.4;
  }

  .workflow-step-accent {
    border-color: rgba(151,206,170,.66);
    background: rgba(91,145,111,.13);
    box-shadow: inset 0 0 0 1px rgba(151,206,170,.08);
  }

  .workflow-arrow {
    display: grid;
    place-items: center;
    color: rgba(240,245,241,.7);
    font-size: .9rem;
  }

  @media (max-width: 860px) {
    .workflow-preview {
      grid-template-columns: 1fr;
    }

    .workflow-step {
      min-height: 112px;
    }

    .workflow-arrow {
      transform: rotate(90deg);
      min-height: 16px;
    }
  }
</style>
