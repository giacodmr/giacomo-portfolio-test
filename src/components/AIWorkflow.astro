<div class="workflow" role="img" aria-label="AI-enabled consulting workflow from business input to reviewed decision output">
  <div class="step">
    <span class="step-index">01</span>
    <strong>Business input</strong>
    <small>Questions · Objectives · Context</small>
  </div>

  <div class="connector" aria-hidden="true">→</div>

  <div class="step">
    <span class="step-index">02</span>
    <strong>Data / Documents</strong>
    <small>Reports · Materials · Internal data</small>
  </div>

  <div class="connector" aria-hidden="true">→</div>

  <div class="step accent-step">
    <span class="step-index">03</span>
    <strong>AI-supported analysis</strong>
    <small>Structure · Analyse · Synthesise</small>
  </div>

  <div class="connector" aria-hidden="true">→</div>

  <div class="step review-step">
    <span class="step-index">04</span>
    <strong>Consultant review</strong>
    <small>Validate · Refine · Apply context</small>
  </div>

  <div class="connector" aria-hidden="true">→</div>

  <div class="step output-step">
    <span class="step-index">05</span>
    <strong>Decision / Output</strong>
    <small>Insights · Recommendations · Action</small>
  </div>
</div>

<style>
  .workflow {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr auto 1fr;
    align-items: stretch;
    gap: 14px;
    padding: 42px 28px;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: #f9faf7;
  }

  .step {
    min-height: 184px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 28px;
    padding: 18px;
    border: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.55);
  }

  .step-index {
    color: var(--text-tertiary);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.08em;
  }

  .step strong {
    font-size: 0.98rem;
    line-height: 1.2;
  }

  .step small {
    color: var(--text-secondary);
    font-size: 0.74rem;
    line-height: 1.4;
  }

  .accent-step {
    border-color: var(--accent-medium);
    background: var(--accent-pale);
  }

  .review-step {
    border-color: var(--text);
  }

  .output-step {
    background: var(--text);
    color: var(--bg);
    border-color: var(--text);
  }

  .output-step .step-index,
  .output-step small {
    color: #c7cbc4;
  }

  .connector {
    display: grid;
    place-items: center;
    color: var(--accent-medium);
    font-size: 1.2rem;
  }

  @media (max-width: 900px) {
    .workflow {
      grid-template-columns: 1fr;
      padding: 24px;
    }

    .connector {
      transform: rotate(90deg);
      min-height: 24px;
    }

    .step {
      min-height: 140px;
    }
  }
</style>
