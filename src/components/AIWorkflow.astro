<div class="workflow-preview" role="img" aria-label="AI-enabled consulting workflow preview">
  <div class="workflow-step">
    <span>01</span>
    <strong>Business question</strong>
    <small>Objectives · Context · Constraints</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>02</span>
    <strong>Data &amp; documents</strong>
    <small>Internal data · Reports · Materials</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step workflow-step-accent">
    <span>03</span>
    <strong>AI-supported analysis</strong>
    <small>Structure · Analyse · Synthesise</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>04</span>
    <strong>Consultant review</strong>
    <small>Validate · Refine · Apply context</small>
  </div>
  <div class="workflow-arrow" aria-hidden="true">→</div>
  <div class="workflow-step">
    <span>05</span>
    <strong>Output / decision</strong>
    <small>Insights · Recommendations · Action</small>
  </div>
</div>

<style>
  .workflow-preview {
    width: 100%;
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr auto 1fr;
    gap: 12px;
    align-items: stretch;
    margin-top: 0;
  }

  .workflow-step {
    min-width: 0;
    min-height: 186px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 18px;
    padding: 18px;
    border: 1px solid var(--ds-graphite-70, #525252);
    border-radius: 10px;
    background: var(--ds-graphite-90, #262626);
  }

  .workflow-step span {
    color: #aeb8c4;
    font-size: .64rem;
    font-weight: 600;
    letter-spacing: .12em;
  }

  .workflow-step strong {
    color: var(--ds-white, #ffffff);
    font-size: .9rem;
    font-weight: 550;
    line-height: 1.24;
  }

  .workflow-step small {
    color: #d0d7de;
    font-size: .67rem;
    line-height: 1.5;
  }

  .workflow-step-accent {
    border-color: var(--ds-blue-300, #78a9ff);
    background: color-mix(in srgb, var(--ds-blue-800, #002d9c) 42%, var(--ds-graphite-90, #262626));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--ds-blue-300, #78a9ff) 18%, transparent);
  }

  .workflow-arrow {
    display: grid;
    place-items: center;
    color: var(--ds-blue-300, #78a9ff);
    font-size: 1.05rem;
  }

  @media (max-width: 860px) {
    .workflow-preview { grid-template-columns: 1fr; gap: 8px; }
    .workflow-step { min-height: 122px; }
    .workflow-arrow { transform: rotate(90deg); min-height: 18px; }
  }
</style>
