<section id="experience" class="experience-section">
  <div class="container experience-grid">
    <p class="eyebrow">Experience</p>

    <div class="experience-main">
      <h2>JAKALA</h2>
      <p>Business Consultant · Client Strategy<br />Milan</p>
      <p class="annotation">Selected as AI Champion — Consumer</p>
    </div>

    <div class="experience-links">
      <a class="editorial-link" href="#">About <span aria-hidden="true">↗</span></a>
      <a class="editorial-link" href="#">View CV <span aria-hidden="true">↗</span></a>
    </div>
  </div>
</section>

<style>
  .experience-section {
    padding-block: clamp(72px, 8vw, 112px);
    border-bottom: 1px solid var(--border);
  }

  .experience-grid {
    display: grid;
    grid-template-columns: 2fr 6fr 4fr;
    gap: 32px;
    align-items: start;
  }

  .experience-main h2 {
    margin: 0;
    font-size: clamp(2rem, 3vw, 2.8rem);
    font-weight: 560;
    letter-spacing: -0.04em;
  }

  .experience-main p {
    margin: 12px 0 0;
    color: var(--text-secondary);
  }

  .experience-main .annotation {
    margin-top: 24px;
    color: var(--text);
    font-size: 0.82rem;
    font-weight: 600;
  }

  .experience-links {
    display: flex;
    justify-content: flex-end;
    gap: 26px;
  }

  @media (max-width: 800px) {
    .experience-grid {
      grid-template-columns: 1fr;
    }

    .experience-links {
      justify-content: flex-start;
      margin-top: 16px;
    }
  }
</style>
