<section id="experience" class="experience-section">
  <div class="container">
    <div class="experience-strip">
      <div class="experience-label">
        <span class="eyebrow">Experience</span>
        <strong>JAKALA</strong>
      </div>

      <div class="experience-role">
        <strong>Business Consultant · Client Strategy</strong>
        <span>Milan</span>
      </div>

      <p class="experience-summary">
        Strategy, analytics and transformation work across consumer and commercial topics, with applied AI as a practical capability.
      </p>

      <a class="experience-link" href="#">
        View CV <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .experience-section { padding: 0 0 48px; background: var(--surface-soft); }
  .experience-strip {
    display: grid;
    grid-template-columns: 1.4fr 2.5fr 3fr auto;
    gap: 28px;
    align-items: center;
    min-height: 124px;
    padding: 24px 28px;
    border: 1px solid var(--border);
    background: var(--surface);
  }
  .experience-label { display: grid; gap: 10px; }
  .experience-label strong { font-size: 1rem; letter-spacing: .03em; }
  .experience-role,
  .experience-summary {
    padding-left: 24px;
    border-left: 1px solid var(--border);
  }
  .experience-role { display: grid; gap: 5px; }
  .experience-role strong { font-size: .92rem; font-weight: 600; }
  .experience-role span,
  .experience-summary { color: var(--text-secondary); font-size: .82rem; line-height: 1.5; }
  .experience-summary { margin: 0; }
  .experience-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--text-secondary);
    font-size: .84rem;
    font-weight: 600;
  }
  .experience-link:hover { color: var(--accent); }

  @media (max-width: 900px) {
    .experience-strip { grid-template-columns: 1fr 1fr; }
    .experience-summary,
    .experience-role { padding-left: 0; border-left: 0; }
  }
  @media (max-width: 620px) {
    .experience-strip { grid-template-columns: 1fr; gap: 18px; padding: 24px; }
  }
</style>
