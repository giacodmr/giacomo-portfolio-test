<footer class="site-footer">
  <div class="container footer-row">
    <div class="footer-brand">
      <strong>Giacomo Di Muro</strong>
      <span>Business Strategy &amp; Analytics · Milan</span>
    </div>

    <nav aria-label="Footer navigation">
      <a href="/#work">Work</a>
      <a href="/#experience">About</a>
      <a href="#">CV</a>
      <a href="#">LinkedIn ↗</a>
    </nav>
  </div>
</footer>

<style>
  .site-footer { padding: 30px 0 36px; border-top: 1px solid var(--border); background: var(--surface); }
  .footer-row { display: flex; justify-content: space-between; align-items: center; gap: 30px; }
  .footer-brand { display: grid; gap: 5px; }
  .footer-brand strong { font-size: .9rem; }
  .footer-brand span { color: var(--text-tertiary); font-size: .72rem; }
  nav { display: flex; flex-wrap: wrap; gap: 24px; }
  nav a { color: var(--text-secondary); font-size: .78rem; font-weight: 550; }
  nav a:hover,
  nav a:focus-visible { color: var(--text); }
  @media (max-width: 700px) {
    .footer-row { align-items: flex-start; flex-direction: column; }
  }
</style>
