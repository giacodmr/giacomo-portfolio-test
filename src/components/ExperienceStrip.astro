<section id="experience" class="experience-section">
  <div class="container">
    <div class="experience-strip">
      <div class="experience-label">
        <span class="eyebrow">Experience</span>
        <strong>JAKALA</strong>
      </div>

      <div class="experience-role">
        <strong>Business Consultant · Client Strategy</strong>
        <span>Milan</span>
      </div>

      <div class="experience-proof">
        <span>Selected as</span>
        <strong>AI Champion — Consumer</strong>
      </div>

      <p class="experience-summary">
        Strategy, analytics and transformation work across consumer and commercial topics, with applied AI as a practical capability.
      </p>

      <a class="experience-link" href="/about">
        About &amp; experience <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .experience-section { padding: 0 0 48px; background: var(--surface-soft); }
  .experience-strip {
    display: grid;
    grid-template-columns: 1.15fr 2.1fr 1.65fr 2.6fr auto;
    gap: 24px;
    align-items: center;
    min-height: 132px;
    padding: 24px 28px;
    border: 1px solid var(--border);
    background: var(--surface);
  }
  .experience-label,
  .experience-role,
  .experience-proof { display: grid; gap: 7px; }
  .experience-label strong { font-size: 1rem; letter-spacing: .03em; }
  .experience-role,
  .experience-proof,
  .experience-summary {
    padding-left: 24px;
    border-left: 1px solid var(--border);
  }
  .experience-role strong,
  .experience-proof strong { font-size: .88rem; font-weight: 600; }
  .experience-role span,
  .experience-proof span,
  .experience-summary { color: var(--text-secondary); font-size: .78rem; line-height: 1.5; }
  .experience-summary { margin: 0; }
  .experience-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--text-secondary);
    font-size: .82rem;
    font-weight: 600;
    white-space: nowrap;
  }
  .experience-link:hover { color: var(--accent); }

  @media (max-width: 1050px) {
    .experience-strip { grid-template-columns: 1fr 1fr; }
    .experience-summary,
    .experience-role,
    .experience-proof { padding-left: 0; border-left: 0; }
  }
  @media (max-width: 620px) {
    .experience-strip { grid-template-columns: 1fr; gap: 18px; padding: 24px; }
  }
</style>
