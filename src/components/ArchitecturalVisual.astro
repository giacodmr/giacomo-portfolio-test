<div class="architecture-panel" role="img" aria-label="Abstract modern business architecture">
  <div class="architecture-copy">
    <span>People</span>
    <span>Insights</span>
    <span>Technology</span>
    <span>Impact</span>
  </div>

  <div class="tower tower-a"></div>
  <div class="tower tower-b"></div>
  <div class="tower tower-c"></div>
  <div class="bridge"></div>
  <div class="terrace"></div>

  <div class="architecture-note">
    <span>Complexity</span>
    <span>into progress</span>
  </div>
</div>

<style>
  .architecture-panel {
    position: relative;
    min-height: 560px;
    overflow: hidden;
    border-radius: 0 0 0 24px;
    background:
      radial-gradient(circle at 24% 18%, color-mix(in srgb, var(--ds-white, #fff) 92%, transparent), transparent 31%),
      linear-gradient(135deg, var(--ds-surface, #fff) 0%, var(--ds-blue-100, #d0e2ff) 52%, color-mix(in srgb, var(--ds-blue-300, #78a9ff) 52%, var(--ds-surface, #fff)) 100%);
    isolation: isolate;
  }

  .architecture-panel::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      linear-gradient(90deg, color-mix(in srgb, var(--ds-white, #fff) 46%, transparent), transparent 38%),
      linear-gradient(180deg, transparent 58%, color-mix(in srgb, var(--ds-graphite-100, #161616) 18%, transparent));
    z-index: 5;
    pointer-events: none;
  }

  .tower {
    position: absolute;
    bottom: -1%;
    border: 1px solid color-mix(in srgb, var(--ds-light, #f4f4f4) 28%, transparent);
    background:
      repeating-linear-gradient(90deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 16%, transparent) 0 1px, transparent 1px 34px),
      repeating-linear-gradient(180deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 13%, transparent) 0 1px, transparent 1px 30px),
      linear-gradient(145deg, color-mix(in srgb, var(--ds-blue-300, #78a9ff) 58%, var(--ds-graphite-70, #525252)), var(--ds-graphite-100, #161616));
    box-shadow:
      inset 0 0 0 1px color-mix(in srgb, var(--ds-white, #fff) 4%, transparent),
      inset -35px 0 70px color-mix(in srgb, var(--ds-graphite-100, #161616) 24%, transparent);
  }

  .tower-a {
    right: -3%;
    width: 39%;
    height: 86%;
    clip-path: polygon(11% 0, 100% 7%, 100% 100%, 0 100%, 0 13%);
  }

  .tower-b {
    right: 33%;
    width: 30%;
    height: 67%;
    clip-path: polygon(18% 4%, 100% 0, 100% 100%, 0 100%, 0 19%);
    background:
      repeating-linear-gradient(90deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 16%, transparent) 0 1px, transparent 1px 32px),
      repeating-linear-gradient(180deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 13%, transparent) 0 1px, transparent 1px 27px),
      linear-gradient(160deg, color-mix(in srgb, var(--ds-blue-300, #78a9ff) 68%, var(--ds-graphite-70, #525252)), color-mix(in srgb, var(--ds-blue-800, #002d9c) 42%, var(--ds-graphite-100, #161616)));
  }

  .tower-c {
    left: 3%;
    width: 35%;
    height: 45%;
    clip-path: polygon(6% 11%, 100% 0, 100% 100%, 0 100%, 0 18%);
    background:
      repeating-linear-gradient(90deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 18%, transparent) 0 1px, transparent 1px 28px),
      repeating-linear-gradient(180deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 13%, transparent) 0 1px, transparent 1px 26px),
      linear-gradient(145deg, color-mix(in srgb, var(--ds-blue-300, #78a9ff) 52%, var(--ds-data-low, #c1c7cd)), color-mix(in srgb, var(--ds-blue-800, #002d9c) 34%, var(--ds-graphite-100, #161616)));
  }

  .bridge {
    position: absolute;
    left: 16%;
    right: 7%;
    bottom: 18%;
    height: 9%;
    background:
      repeating-linear-gradient(90deg, color-mix(in srgb, var(--ds-light, #f4f4f4) 9%, transparent) 0 1px, transparent 1px 30px),
      linear-gradient(180deg, var(--ds-blue-800, #002d9c), var(--ds-graphite-100, #161616));
    clip-path: polygon(3% 8%, 100% 0, 99% 76%, 0 100%);
    z-index: 3;
  }

  .terrace {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 17%;
    background: linear-gradient(180deg, color-mix(in srgb, var(--ds-blue-100, #d0e2ff) 62%, var(--ds-surface, #fff)), color-mix(in srgb, var(--ds-graphite-70, #525252) 70%, var(--ds-blue-300, #78a9ff)));
    z-index: 2;
  }

  .architecture-copy {
    position: absolute;
    z-index: 7;
    left: 7%;
    top: 10%;
    display: grid;
    gap: 4px;
    color: var(--ds-blue-800, #002d9c);
    font-size: .68rem;
    font-weight: 600;
    letter-spacing: .2em;
    text-transform: uppercase;
  }

  .architecture-copy::after {
    content: "";
    width: 34px;
    height: 1px;
    margin-top: 12px;
    background: color-mix(in srgb, var(--ds-blue-800, #002d9c) 46%, transparent);
  }

  .architecture-note {
    position: absolute;
    z-index: 7;
    right: 6%;
    bottom: 7%;
    display: grid;
    gap: 2px;
    color: color-mix(in srgb, var(--ds-white, #fff) 90%, transparent);
    font-size: .68rem;
    font-weight: 600;
    letter-spacing: .2em;
    text-transform: uppercase;
    text-align: right;
  }

  @media (max-width: 980px) {
    .architecture-panel {
      min-height: 430px;
      border-radius: 20px;
    }
  }
</style>
