<section id="experience" class="experience-section">
  <div class="container experience-strip">
    <div class="experience-label">
      <span class="eyebrow">Experience</span>
      <strong>JAKALA</strong>
    </div>

    <div class="experience-role">
      <strong>Business Consultant · Client Strategy</strong>
      <span>Milan</span>
    </div>

    <p class="experience-summary">
      Supporting strategy, analytics and transformation work across consumer and commercial topics.
    </p>

    <a class="experience-link" href="#">
      More about me <span aria-hidden="true">→</span>
    </a>
  </div>
</section>

<style>
  .experience-section {
    padding: 0 0 22px;
  }

  .experience-strip {
    display: grid;
    grid-template-columns: 1.6fr 2.8fr 3.2fr auto;
    gap: 28px;
    align-items: center;
    min-height: 112px;
    padding: 22px 28px;
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    background: rgba(251, 250, 246, .7);
  }

  .experience-label {
    display: grid;
    gap: 10px;
  }

  .experience-label strong {
    font-size: 1.12rem;
    letter-spacing: .03em;
  }

  .experience-role {
    display: grid;
    gap: 4px;
    padding-left: 24px;
    border-left: 1px solid var(--border-light);
  }

  .experience-role strong {
    font-size: .84rem;
    font-weight: 500;
  }

  .experience-role span {
    color: var(--text-secondary);
    font-size: .72rem;
  }

  .experience-summary {
    margin: 0;
    padding-left: 24px;
    border-left: 1px solid var(--border-light);
    color: var(--text-secondary);
    font-size: .72rem;
    line-height: 1.5;
  }

  .experience-link {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    font-size: .75rem;
    font-weight: 600;
  }

  .experience-link span {
    transition: transform .18s var(--ease);
  }

  .experience-link:hover span {
    transform: translateX(4px);
  }

  @media (max-width: 900px) {
    .experience-strip {
      grid-template-columns: 1fr 1fr;
    }

    .experience-summary,
    .experience-role {
      padding-left: 0;
      border-left: 0;
    }
  }

  @media (max-width: 620px) {
    .experience-strip {
      grid-template-columns: 1fr;
      gap: 18px;
    }
  }
</style>
