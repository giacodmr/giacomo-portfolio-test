<div class="retail-report" role="img" aria-label="Synthetic retail performance report card combining a store distribution histogram, horizontal average-ticket bars, and a purchase-frequency dot plot">
  <section class="viz-panel distribution-panel">
    <header><span>01</span><strong>Store performance distribution</strong></header>
    <div class="histogram" aria-hidden="true">
      <i style="--h: 27%"></i><i style="--h: 46%"></i><i style="--h: 69%"></i><i style="--h: 88%"></i><i style="--h: 100%"></i><i style="--h: 82%"></i><i style="--h: 61%"></i><i class="focus" style="--h: 39%"></i><i class="focus-strong" style="--h: 22%"></i>
    </div>
    <div class="distribution-axis" aria-hidden="true"><span>Lower</span><span>Core</span><span>Top</span></div>
    <p><b>Top 20%</b> of stores separated from the network baseline.</p>
  </section>

  <section class="viz-panel ticket-panel">
    <header><span>02</span><strong>Average ticket index</strong></header>
    <div class="horizontal-bars" aria-hidden="true">
      <div class="hbar-row"><span>Low</span><i><b style="--w: 68%"></b></i><strong>82</strong></div>
      <div class="hbar-row"><span>Core</span><i><b style="--w: 78%"></b></i><strong>94</strong></div>
      <div class="hbar-row"><span>High</span><i><b class="accent" style="--w: 89%"></b></i><strong>108</strong></div>
      <div class="hbar-row"><span>Top</span><i><b class="focus" style="--w: 100%"></b></i><strong>121</strong></div>
    </div>
    <p>Index increases consistently across performance clusters.</p>
  </section>

  <section class="viz-panel frequency-panel">
    <header><span>03</span><strong>Purchase frequency index</strong></header>
    <div class="dotplot-wrap" aria-hidden="true">
      <svg viewBox="0 0 260 112" preserveAspectRatio="none">
        <g class="grid-lines"><line x1="16" x2="244" y1="88" y2="88" /><line x1="16" x2="244" y1="58" y2="58" /><line x1="16" x2="244" y1="28" y2="28" /></g>
        <polyline class="trend-line" points="28,82 92,63 156,46 224,24" />
        <circle class="dot neutral" cx="28" cy="82" r="5" /><circle class="dot neutral" cx="92" cy="63" r="5" /><circle class="dot accent" cx="156" cy="46" r="5.5" /><circle class="dot focus" cx="224" cy="24" r="6" />
        <text x="28" y="70">75</text><text x="92" y="51">91</text><text x="156" y="34">104</text><text x="224" y="12">118</text>
      </svg>
      <div class="dotplot-axis"><span>Low</span><span>Core</span><span>High</span><span>Top</span></div>
    </div>
    <p>Higher-performing clusters show stronger repeat-purchase behaviour.</p>
  </section>

  <p class="report-note">Illustrative reconstruction with synthetic values.</p>
</div>

<style>
  .retail-report { display: grid; grid-template-columns: 1.05fr 1fr 1.05fr; gap: 0; margin-top: 22px; border-top: 1px solid var(--ds-border-light, rgba(20, 26, 22, .08)); border-bottom: 1px solid var(--ds-border-light, rgba(20, 26, 22, .08)); }
  .viz-panel { min-width: 0; padding: 18px 20px 17px; border-right: 1px solid var(--ds-border-light, rgba(20, 26, 22, .08)); }
  .viz-panel:first-child { padding-left: 0; }
  .viz-panel:nth-child(3) { padding-right: 0; border-right: 0; }
  .viz-panel header { display: flex; align-items: baseline; gap: 8px; min-height: 34px; }
  .viz-panel header span { color: var(--ds-subtle, #8b928c); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .55rem; font-weight: 600; letter-spacing: .12em; }
  .viz-panel header strong { color: var(--ds-muted, #626a63); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .68rem; font-weight: 600; line-height: 1.25; }
  .viz-panel > p { min-height: 30px; margin: 12px 0 0; color: var(--ds-subtle, #8b928c); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .55rem; line-height: 1.35; }
  .viz-panel > p b { color: var(--ds-ink, #141a16); font-weight: 600; }
  .histogram { height: 110px; display: flex; align-items: end; gap: 4px; padding-top: 13px; }
  .histogram i { flex: 1; height: var(--h); min-height: 10px; background: var(--ds-data-context, #bdc9c0); border-radius: 2px 2px 0 0; }
  .histogram i:nth-child(4), .histogram i:nth-child(5), .histogram i:nth-child(6) { background: color-mix(in srgb, var(--ds-data-secondary, #6f9a82) 55%, var(--ds-data-context, #bdc9c0)); }
  .histogram i.focus { background: var(--ds-data-secondary, #6f9a82); }
  .histogram i.focus-strong { background: var(--ds-data-focus, #0e563f); }
  .distribution-axis, .dotplot-axis { display: flex; justify-content: space-between; margin-top: 7px; color: var(--ds-subtle, #8b928c); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .5rem; }
  .horizontal-bars { display: grid; gap: 9px; padding-top: 16px; min-height: 117px; }
  .hbar-row { display: grid; grid-template-columns: 29px 1fr 24px; align-items: center; gap: 7px; min-width: 0; }
  .hbar-row > span, .hbar-row > strong { color: var(--ds-subtle, #8b928c); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .52rem; font-weight: 500; }
  .hbar-row > strong { color: var(--ds-ink, #141a16); text-align: right; font-weight: 600; }
  .hbar-row > i { position: relative; height: 8px; overflow: hidden; border-radius: 2px; background: var(--ds-surface-soft, #f0eee7); }
  .hbar-row > i > b { display: block; width: var(--w); height: 100%; border-radius: inherit; background: var(--ds-data-context, #bdc9c0); }
  .hbar-row > i > b.accent { background: var(--ds-data-secondary, #6f9a82); }
  .hbar-row > i > b.focus { background: var(--ds-data-focus, #0e563f); }
  .dotplot-wrap { padding-top: 8px; }
  .dotplot-wrap svg { display: block; width: 100%; height: 112px; overflow: visible; }
  .grid-lines line { stroke: var(--ds-border-light, rgba(20, 26, 22, .08)); stroke-width: 1; vector-effect: non-scaling-stroke; }
  .trend-line { fill: none; stroke: var(--ds-data-secondary, #6f9a82); stroke-width: 1.7; vector-effect: non-scaling-stroke; }
  .dot { stroke: var(--ds-surface, #fbfaf6); stroke-width: 2; vector-effect: non-scaling-stroke; }
  .dot.neutral { fill: var(--ds-data-context, #bdc9c0); } .dot.accent { fill: var(--ds-data-secondary, #6f9a82); } .dot.focus { fill: var(--ds-data-focus, #0e563f); }
  .dotplot-wrap text { fill: var(--ds-ink, #141a16); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: 9px; font-weight: 600; text-anchor: middle; }
  .dotplot-axis { display: grid; grid-template-columns: repeat(4, 1fr); text-align: center; margin-top: 0; }
  .report-note { grid-column: 1 / -1; margin: 0; padding: 9px 0; border-top: 1px solid var(--ds-border-light, rgba(20, 26, 22, .08)); color: var(--ds-subtle, #8b928c); font-family: var(--ds-font-sans, 'IBM Plex Sans', sans-serif); font-size: .53rem; }
  @media (max-width: 760px) { .retail-report { grid-template-columns: 1fr; border-bottom: 0; } .viz-panel, .viz-panel:first-child, .viz-panel:nth-child(3) { padding: 18px 0; border-right: 0; border-bottom: 1px solid var(--ds-border-light, rgba(20, 26, 22, .08)); } .report-note { padding-block: 10px; } }
</style>
